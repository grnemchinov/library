<Window x:Class="LibraryPraktos.JSONWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryPraktos"
        mc:Ignorable="d"
        Title="JSONWin" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="NumOf" Grid.Row="1" Grid.Column="0" Margin="50"/>
        <Button Grid.Row="2" Grid.Column="0" Margin="20" Content="Создать N объектов" Click="Button_Click"/>
        <ListBox x:Name="Data" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
        <Button x:Name="GetData" Grid.Row="2" Grid.Column="2" Margin="40" Content="Выгрузить информацию" Click="GetData_Click"/>
    </Grid>
</Window>
